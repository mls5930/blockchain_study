// if(process.platform === "linux") {
//     console.log("나를 실행시킨 플랫폼(운영체제)가 리눅스가 맞는가?");
// }

// 내가 실행한 위치 + index6.js를 출력하고 싶다.

// __dirname(directory name) : 현재 파일이 위치한 디렉토리(폴더) 경로
// __filename : 현재 파일의 전체 경로(디렉토리 + 현재 실행시키는 파일이름)

// console.log(`${__dirname}/index6.js`);
console.log(__filename);
